#pragma version 11
#pragma typetrack false

// algopy.arc4.ARC4Contract.approval_program() -> uint64:
main:
    intcblock 8 0 1 2 132 130 1000000000 58100
    bytecblock 0x75 0x151f7c75 "frozen" 0x0082 "next_upkeep_id"
    txn ApplicationID
    bnz main_after_if_else@2
    // smart_contracts/keeper/contract.py:111
    // self.next_upkeep_id = GlobalState(UInt64(0))
    bytec 4 // "next_upkeep_id"
    intc_1 // 0
    app_global_put
    // smart_contracts/keeper/contract.py:112-116
    // # 0 while the creator may still replace the programs, 1 once that is
    // # given up for good. Global state, so anyone can read it before they
    // # escrow anything: the promise is only worth what it can be checked
    // # against. See `freeze`.
    // self.frozen = GlobalState(UInt64(0))
    bytec_2 // "frozen"
    intc_1 // 0
    app_global_put

main_after_if_else@2:
    // smart_contracts/keeper/contract.py:101
    // class Keeper(ARC4Contract):
    txn NumAppArgs
    bz main___algopy_default_create@18
    pushbytes 0xa0e81872 // method "update()void"
    txna ApplicationArgs 0
    match main_update_route@5

main_switch_case_next@6:
    // smart_contracts/keeper/contract.py:101
    // class Keeper(ARC4Contract):
    txn OnCompletion
    !
    assert
    txn ApplicationID
    assert
    pushbytess 0x0e2c6c0f 0x3636cfc6 0xf9f6382d 0x36966bf8 0x5b49cc5c 0x5c67ed76 0xd44aac6b // method "freeze()void", method "register(pay,pay,uint64,byte[][],uint64,uint64,uint64,uint64,uint64,uint64)uint64", method "top_up(uint64,pay)uint64", method "cancel(uint64)uint64", method "execute(uint64)uint64", method "opt_in_asset(pay,uint64,uint64)uint64", method "top_up_asset(uint64,axfer)uint64"
    txna ApplicationArgs 0
    match freeze register top_up cancel execute opt_in_asset top_up_asset
    err

main_update_route@5:
    // smart_contracts/keeper/contract.py:118
    // @abimethod(allow_actions=["UpdateApplication"])
    txn OnCompletion
    pushint 4 // UpdateApplication
    ==
    txn ApplicationID
    &&
    assert
    b update

main___algopy_default_create@18:
    txn OnCompletion
    !
    txn ApplicationID
    !
    &&
    return


// smart_contracts.keeper.contract.Keeper.update[routing]() -> void:
update:
    // smart_contracts/keeper/contract.py:132
    // assert Txn.sender == Global.creator_address, "Only the creator can update"
    txn Sender
    global CreatorAddress
    ==
    assert // Only the creator can update
    // smart_contracts/keeper/contract.py:133
    // assert self.frozen.value == 0, "Frozen: the programs cannot be replaced"
    intc_1 // 0
    bytec_2 // "frozen"
    app_global_get_ex
    assert // check self.frozen exists
    !
    assert // Frozen: the programs cannot be replaced
    // smart_contracts/keeper/contract.py:118
    // @abimethod(allow_actions=["UpdateApplication"])
    intc_2 // 1
    return


// smart_contracts.keeper.contract.Keeper.freeze[routing]() -> void:
freeze:
    // smart_contracts/keeper/contract.py:145
    // assert Txn.sender == Global.creator_address, "Only the creator can freeze"
    txn Sender
    global CreatorAddress
    ==
    assert // Only the creator can freeze
    // smart_contracts/keeper/contract.py:146
    // assert self.frozen.value == 0, "Already frozen"
    intc_1 // 0
    bytec_2 // "frozen"
    app_global_get_ex
    assert // check self.frozen exists
    !
    assert // Already frozen
    // smart_contracts/keeper/contract.py:147
    // self.frozen.value = UInt64(1)
    bytec_2 // "frozen"
    intc_2 // 1
    app_global_put
    // smart_contracts/keeper/contract.py:135
    // @abimethod()
    intc_2 // 1
    return


// smart_contracts.keeper.contract.Keeper.register[routing]() -> void:
register:
    pushbytes ""
    // smart_contracts/keeper/contract.py:149
    // @abimethod()
    txn GroupIndex
    intc_3 // 2
    -
    dup
    gtxns TypeEnum
    intc_2 // pay
    ==
    assert // transaction type is pay
    txn GroupIndex
    intc_2 // 1
    -
    dup
    gtxns TypeEnum
    intc_2 // pay
    ==
    assert // transaction type is pay
    txna ApplicationArgs 1
    dup
    cover 2
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    txna ApplicationArgs 2
    dup
    cover 4
    dup
    intc_1 // 0
    extract_uint16 // on error: invalid array length header
    dup
    cover 6
    intc_3 // 2
    *
    swap
    dup
    len
    swap
    extract 2 0
    intc_1 // 0

register_for_header@1:
    // smart_contracts/keeper/contract.py:149
    // @abimethod()
    dup
    dig 9
    <
    bz register_after_for@4
    dup
    intc_3 // 2
    *
    dig 2
    dup
    uncover 2
    extract_uint16 // on error: invalid array encoding
    dup
    uncover 6
    dup
    cover 4
    ==
    assert // invalid tail pointer for (len+(len+uint8[])[])
    dig 1
    len
    substring3
    intc_1 // 0
    extract_uint16 // on error: invalid array length header
    intc_3 // 2
    +
    +
    cover 3
    intc_2 // 1
    +
    b register_for_header@1

register_after_for@4:
    popn 2
    // smart_contracts/keeper/contract.py:149
    // @abimethod()
    swap
    intc_3 // 2
    +
    dig 1
    ==
    assert // invalid number of bytes for arc4.dynamic_array<arc4.dynamic_array<arc4.uint8>>
    txna ApplicationArgs 3
    dup
    cover 5
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    dup
    cover 5
    txna ApplicationArgs 4
    dup
    cover 7
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    dup
    cover 3
    txna ApplicationArgs 5
    dup
    cover 9
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    txna ApplicationArgs 6
    dup
    cover 10
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    dup
    cover 5
    txna ApplicationArgs 7
    dup
    cover 11
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    cover 6
    txna ApplicationArgs 8
    dup
    cover 11
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    bury 21
    // smart_contracts/keeper/contract.py:171
    // assert interval_rounds >= MIN_INTERVAL_ROUNDS, "Interval below minimum"
    dig 3
    pushint 10
    >=
    assert // Interval below minimum
    // smart_contracts/keeper/contract.py:172
    // assert interval_rounds <= MAX_INTERVAL_ROUNDS, "Interval above maximum"
    uncover 3
    intc 6 // 1000000000
    <=
    assert // Interval above maximum
    // smart_contracts/keeper/contract.py:173
    // assert fee_per_execution >= MIN_UPKEEP_FEE, "Fee below minimum"
    dig 2
    pushint 4000
    >=
    assert // Fee below minimum
    // smart_contracts/keeper/contract.py:174
    // assert fee_per_execution <= MAX_UPKEEP_FEE, "Fee above maximum"
    uncover 2
    intc 6 // 1000000000
    <=
    assert // Fee above maximum
    // smart_contracts/keeper/contract.py:175
    // assert policy <= SKIP_AHEAD, "Unknown catch-up policy"
    swap
    intc_2 // 1
    <=
    assert // Unknown catch-up policy
    // smart_contracts/keeper/contract.py:176-179
    // # A cap below the base fee would mean the escalation curve runs
    // # backwards; reject it rather than clamping something the creator did
    // # not ask for.
    // assert fee_cap == 0 or fee_cap >= fee_per_execution, "Fee cap below the fee"
    bz register_bool_true@7
    dig 1
    dig 4
    >=
    bz register_bool_false@8

register_bool_true@7:
    intc_2 // 1

register_bool_merge@9:
    // smart_contracts/keeper/contract.py:176-179
    // # A cap below the base fee would mean the escalation curve runs
    // # backwards; reject it rather than clamping something the creator did
    // # not ask for.
    // assert fee_cap == 0 or fee_cap >= fee_per_execution, "Fee cap below the fee"
    assert // Fee cap below the fee
    // smart_contracts/keeper/contract.py:180
    // assert fee_cap <= MAX_UPKEEP_FEE, "Fee cap above maximum"
    dig 1
    intc 6 // 1000000000
    <=
    assert // Fee cap above maximum
    // smart_contracts/keeper/contract.py:181-183
    // # A bonus of nothing is a nonsense state that pays 24 bytes of box MBR
    // # for a feature it does not use; reject it rather than store it.
    // assert fee_asset == 0 or asset_fee > 0, "Asset fee must be positive"
    uncover 2
    bz register_bool_true@11
    dig 15
    bz register_bool_false@12

register_bool_true@11:
    intc_2 // 1

register_bool_merge@13:
    // smart_contracts/keeper/contract.py:181-183
    // # A bonus of nothing is a nonsense state that pays 24 bytes of box MBR
    // # for a feature it does not use; reject it rather than store it.
    // assert fee_asset == 0 or asset_fee > 0, "Asset fee must be positive"
    assert // Asset fee must be positive
    // smart_contracts/keeper/contract.py:185-188
    // # Bounded here rather than in `execute`: an argument list longer than
    // # the fan-out would register happily and then fail on every execution,
    // # for good, which is the same shape as the fee-cap trap.
    // assert UInt64(0) < arg_count <= MAX_CALL_ARGS, "Argument count out of bounds"
    dig 14
    bz register_bool_false@16
    dig 14
    pushint 3
    <=
    bz register_bool_false@16
    intc_2 // 1

register_bool_merge@17:
    // smart_contracts/keeper/contract.py:185-188
    // # Bounded here rather than in `execute`: an argument list longer than
    // # the fan-out would register happily and then fail on every execution,
    // # for good, which is the same shape as the fee-cap trap.
    // assert UInt64(0) < arg_count <= MAX_CALL_ARGS, "Argument count out of bounds"
    assert // Argument count out of bounds
    // smart_contracts/keeper/contract.py:190
    // assert size <= MAX_CALL_DATA, "Argument list too large"
    dup
    pushint 1024
    <=
    assert // Argument list too large
    // smart_contracts/keeper/contract.py:192
    // required_mbr = BOX_MBR_FIXED + 400 * size
    pushint 400
    *
    intc 7 // 58100
    +
    // smart_contracts/keeper/contract.py:194
    // mbr_payment.receiver == Global.current_application_address
    uncover 6
    dup
    gtxns Receiver
    global CurrentApplicationAddress
    ==
    // smart_contracts/keeper/contract.py:193-195
    // assert (
    //     mbr_payment.receiver == Global.current_application_address
    // ), "MBR payment must fund the app account"
    assert // MBR payment must fund the app account
    // smart_contracts/keeper/contract.py:196-212
    // # A rekey hands control of the sender's account to whoever the group
    // # names, and a close sweeps it empty to whoever the group names.
    // # Both harm only the sender, so the contract loses nothing by
    // # refusing them. The exposure is a front end putting either into a
    // # group a user signs without reading it closely.
    // # The upkeep's creator is `Txn.sender`, and `cancel` pays the creator.
    // # Without this, those are two different accounts and nothing requires
    // # them to match: a victim signs both payments to the genuine app
    // # account of the genuine app id, an attacker signs the app call, and
    // # the attacker owns the upkeep and cancels it for the escrow plus the
    // # released box MBR. Every check anyone is taught to make passes,
    // # because the receiver and the app id really are the right ones. The
    // # sender of the app call is the only thing that differs, and it is the
    // # one thing no wallet highlights. `subscription.subscribe` has bound
    // # its payer since it was written; the contract holding the money did
    // # not.
    // assert mbr_payment.sender == Txn.sender, "MBR payment must come from the caller"
    dup
    gtxns Sender
    txn Sender
    ==
    assert // MBR payment must come from the caller
    // smart_contracts/keeper/contract.py:214
    // funding_payment.sender == Txn.sender
    dig 4
    dup
    cover 2
    gtxns Sender
    txn Sender
    ==
    // smart_contracts/keeper/contract.py:213-215
    // assert (
    //     funding_payment.sender == Txn.sender
    // ), "Funding payment must come from the caller"
    assert // Funding payment must come from the caller
    // smart_contracts/keeper/contract.py:216
    // assert mbr_payment.rekey_to == Global.zero_address, "MBR payment must not rekey"
    dup
    gtxns RekeyTo
    global ZeroAddress
    ==
    assert // MBR payment must not rekey
    // smart_contracts/keeper/contract.py:218
    // mbr_payment.close_remainder_to == Global.zero_address
    dup
    gtxns CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/keeper/contract.py:217-219
    // assert (
    //     mbr_payment.close_remainder_to == Global.zero_address
    // ), "MBR payment must not close"
    assert // MBR payment must not close
    // smart_contracts/keeper/contract.py:220
    // assert mbr_payment.amount >= required_mbr, "MBR payment too small"
    gtxns Amount
    uncover 2
    >=
    assert // MBR payment too small
    // smart_contracts/keeper/contract.py:222
    // funding_payment.receiver == Global.current_application_address
    dup
    gtxns Receiver
    global CurrentApplicationAddress
    ==
    // smart_contracts/keeper/contract.py:221-223
    // assert (
    //     funding_payment.receiver == Global.current_application_address
    // ), "Funding must go to the app account"
    assert // Funding must go to the app account
    // smart_contracts/keeper/contract.py:224
    // assert funding_payment.rekey_to == Global.zero_address, "Funding must not rekey"
    dup
    gtxns RekeyTo
    global ZeroAddress
    ==
    assert // Funding must not rekey
    // smart_contracts/keeper/contract.py:226
    // funding_payment.close_remainder_to == Global.zero_address
    gtxns CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/keeper/contract.py:225-227
    // assert (
    //     funding_payment.close_remainder_to == Global.zero_address
    // ), "Funding must not close"
    assert // Funding must not close
    // smart_contracts/keeper/contract.py:234
    // if fee_cap > fee_per_execution:
    dup
    dig 2
    >
    bnz register_if_body@18
    pop

register_after_if_else@19:
    // smart_contracts/keeper/contract.py:237
    // funding_payment.amount >= required_funding
    swap
    gtxns Amount
    dup
    uncover 2
    >=
    // smart_contracts/keeper/contract.py:236-238
    // assert (
    //     funding_payment.amount >= required_funding
    // ), "Funding must cover at least one execution"
    assert // Funding must cover at least one execution
    // smart_contracts/keeper/contract.py:240
    // upkeep_id = self.next_upkeep_id.value
    intc_1 // 0
    bytec 4 // "next_upkeep_id"
    app_global_get_ex
    assert // check self.next_upkeep_id exists
    // smart_contracts/keeper/contract.py:241
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    dup
    itob
    bytec_0 // 0x75
    dig 1
    concat
    // smart_contracts/keeper/contract.py:243
    // creator=arc4.Address(Txn.sender),
    txn Sender
    // smart_contracts/keeper/contract.py:247
    // next_execution_round=arc4.UInt64(Global.round + interval_rounds),
    global Round
    uncover 9
    +
    itob
    // smart_contracts/keeper/contract.py:249
    // balance=arc4.UInt64(funding_payment.amount),
    uncover 5
    itob
    // smart_contracts/keeper/contract.py:250
    // times_executed=arc4.UInt64(0),
    intc_1 // 0
    itob
    // smart_contracts/keeper/contract.py:253-255
    // # Never serviced, so the first execution is measured from now and
    // # arrives exactly one interval later: on time, at the base fee.
    // last_serviced_round=arc4.UInt64(Global.round),
    global Round
    itob
    // smart_contracts/keeper/contract.py:242-259
    // box.value = Upkeep(
    //     creator=arc4.Address(Txn.sender),
    //     target_app=arc4.UInt64(target_app.id),
    //     call_args=call_args.copy(),
    //     interval_rounds=arc4.UInt64(interval_rounds),
    //     next_execution_round=arc4.UInt64(Global.round + interval_rounds),
    //     fee_per_execution=arc4.UInt64(fee_per_execution),
    //     balance=arc4.UInt64(funding_payment.amount),
    //     times_executed=arc4.UInt64(0),
    //     policy=arc4.UInt64(policy),
    //     fee_cap=arc4.UInt64(fee_cap),
    //     # Never serviced, so the first execution is measured from now and
    //     # arrives exactly one interval later: on time, at the base fee.
    //     last_serviced_round=arc4.UInt64(Global.round),
    //     fee_asset=arc4.UInt64(fee_asset),
    //     asset_fee=arc4.UInt64(asset_fee),
    //     asset_balance=arc4.UInt64(0),
    // )
    uncover 4
    uncover 8
    concat
    bytec_3 // 0x0082
    concat
    uncover 13
    concat
    uncover 4
    concat
    uncover 11
    concat
    uncover 3
    concat
    dig 2
    concat
    uncover 9
    concat
    uncover 8
    concat
    swap
    concat
    uncover 6
    concat
    uncover 5
    concat
    swap
    concat
    uncover 4
    concat
    dig 1
    box_del
    pop
    box_put
    // smart_contracts/keeper/contract.py:260
    // self.next_upkeep_id.value = upkeep_id + 1
    swap
    intc_2 // 1
    +
    bytec 4 // "next_upkeep_id"
    swap
    app_global_put
    // smart_contracts/keeper/contract.py:149
    // @abimethod()
    bytec_1 // 0x151f7c75
    swap
    concat
    log
    intc_2 // 1
    return

register_if_body@18:
    bury 1
    b register_after_if_else@19

register_bool_false@16:
    intc_1 // 0
    b register_bool_merge@17

register_bool_false@12:
    intc_1 // 0
    b register_bool_merge@13

register_bool_false@8:
    intc_1 // 0
    b register_bool_merge@9


// smart_contracts.keeper.contract.Keeper.top_up[routing]() -> void:
top_up:
    // smart_contracts/keeper/contract.py:263
    // @abimethod()
    txna ApplicationArgs 1
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    txn GroupIndex
    intc_2 // 1
    -
    dup
    gtxns TypeEnum
    intc_2 // pay
    ==
    assert // transaction type is pay
    // smart_contracts/keeper/contract.py:269
    // funding_payment.receiver == Global.current_application_address
    dup
    gtxns Receiver
    global CurrentApplicationAddress
    ==
    // smart_contracts/keeper/contract.py:268-270
    // assert (
    //     funding_payment.receiver == Global.current_application_address
    // ), "Funding must go to the app account"
    assert // Funding must go to the app account
    // smart_contracts/keeper/contract.py:279
    // funding_payment.sender == Txn.sender
    dup
    gtxns Sender
    txn Sender
    ==
    // smart_contracts/keeper/contract.py:271-280
    // # Bound to the caller, like `register`. Topping up is deliberately
    // # permissionless and stays so: a well-wisher can still fund somebody
    // # else's upkeep, they simply have to pay for it themselves. What this
    // # stops is the group where a victim signs the payment and an attacker
    // # signs the call naming their own upkeep, which only they can cancel.
    // # `register` got this assert and these three did not, which is the
    // # same fix-one-site failure three reviewers have now named.
    // assert (
    //     funding_payment.sender == Txn.sender
    // ), "Funding payment must come from the caller"
    assert // Funding payment must come from the caller
    // smart_contracts/keeper/contract.py:281
    // assert funding_payment.rekey_to == Global.zero_address, "Funding must not rekey"
    dup
    gtxns RekeyTo
    global ZeroAddress
    ==
    assert // Funding must not rekey
    // smart_contracts/keeper/contract.py:283
    // funding_payment.close_remainder_to == Global.zero_address
    dup
    gtxns CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/keeper/contract.py:282-284
    // assert (
    //     funding_payment.close_remainder_to == Global.zero_address
    // ), "Funding must not close"
    assert // Funding must not close
    // smart_contracts/keeper/contract.py:285
    // assert funding_payment.amount > 0, "Amount must be positive"
    gtxns Amount
    dup
    assert // Amount must be positive
    // smart_contracts/keeper/contract.py:287
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    swap
    itob
    bytec_0 // 0x75
    swap
    concat
    // smart_contracts/keeper/contract.py:288
    // assert box, "Upkeep not found"
    dup
    box_len
    bury 1
    assert // Upkeep not found
    // smart_contracts/keeper/contract.py:289
    // upkeep = box.value.copy()
    dup
    box_get
    pop
    swap
    // smart_contracts/keeper/contract.py:289-290
    // upkeep = box.value.copy()
    // new_balance: UInt64 = upkeep.balance.as_uint64() + funding_payment.amount
    dup
    pushint 66
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:290
    // new_balance: UInt64 = upkeep.balance.as_uint64() + funding_payment.amount
    btoi
    uncover 3
    +
    // smart_contracts/keeper/contract.py:291-293
    // box.value = upkeep._replace(
    //     balance=arc4.UInt64(new_balance), call_args=upkeep.call_args.copy()
    // )
    dig 1
    intc_1 // 0
    pushint 32
    box_extract // on error: index out of bounds
    dig 2
    pushint 32
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:292
    // balance=arc4.UInt64(new_balance), call_args=upkeep.call_args.copy()
    dig 4
    pushint 40
    extract_uint16
    dig 5
    len
    uncover 6
    cover 2
    substring3
    // smart_contracts/keeper/contract.py:291-293
    // box.value = upkeep._replace(
    //     balance=arc4.UInt64(new_balance), call_args=upkeep.call_args.copy()
    // )
    dig 4
    pushint 42
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 5
    pushint 50
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 6
    pushint 58
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:292
    // balance=arc4.UInt64(new_balance), call_args=upkeep.call_args.copy()
    uncover 6
    itob
    // smart_contracts/keeper/contract.py:291-293
    // box.value = upkeep._replace(
    //     balance=arc4.UInt64(new_balance), call_args=upkeep.call_args.copy()
    // )
    dig 7
    pushint 74
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 8
    pushint 82
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 9
    pushint 90
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 10
    pushint 98
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 11
    pushint 106
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 12
    pushint 114
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 13
    pushint 122
    intc_0 // 8
    box_extract // on error: index out of bounds
    uncover 13
    uncover 13
    concat
    bytec_3 // 0x0082
    concat
    uncover 11
    concat
    uncover 10
    concat
    uncover 9
    concat
    dig 8
    concat
    uncover 7
    concat
    uncover 6
    concat
    uncover 5
    concat
    uncover 4
    concat
    uncover 3
    concat
    uncover 2
    concat
    swap
    concat
    uncover 2
    concat
    dig 2
    box_del
    pop
    uncover 2
    swap
    box_put
    // smart_contracts/keeper/contract.py:263
    // @abimethod()
    bytec_1 // 0x151f7c75
    swap
    concat
    log
    intc_2 // 1
    return


// smart_contracts.keeper.contract.Keeper.cancel[routing]() -> void:
cancel:
    pushbytes ""
    // smart_contracts/keeper/contract.py:296
    // @abimethod()
    txna ApplicationArgs 1
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    // smart_contracts/keeper/contract.py:304
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    itob
    bytec_0 // 0x75
    swap
    concat
    dupn 2
    // smart_contracts/keeper/contract.py:305
    // assert box, "Upkeep not found"
    box_len
    bury 1
    assert // Upkeep not found
    // smart_contracts/keeper/contract.py:306
    // upkeep = box.value.copy()
    dup
    box_get
    pop
    swap
    // smart_contracts/keeper/contract.py:306-307
    // upkeep = box.value.copy()
    // assert upkeep.creator.native == Txn.sender, "Only the creator can cancel"
    dup
    intc_1 // 0
    pushint 32
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:307
    // assert upkeep.creator.native == Txn.sender, "Only the creator can cancel"
    txn Sender
    ==
    assert // Only the creator can cancel
    // smart_contracts/keeper/contract.py:308-311
    // # The unspent bonus goes back too, which the creator can only receive
    // # if they hold the asset. Checked before anything is refunded, so a
    // # creator who cannot take the ASA does not lose the ALGO as well.
    // bonus_asset: UInt64 = upkeep.fee_asset.as_uint64()
    dup
    pushint 106
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    cover 3
    // smart_contracts/keeper/contract.py:312-322
    // # Pay out no more of the bonus than the app actually holds. The book
    // # value can exceed the real holding: an ASA with a clawback address
    // # can be taken back out of this account by its issuer, and a frozen
    // # one cannot be sent at all. Trusting the book value would make the
    // # asset transfer fail, and because it shares a transaction with the
    // # ALGO refund, the refund would fail with it. The creator would lose
    // # their escrow and their box minimum balance to somebody else's asset
    // # settings, permanently, on a contract with no delete path.
    // #
    // # So the ASA is best effort and the ALGO is not.
    // bonus: UInt64 = upkeep.asset_balance.as_uint64()
    pushint 122
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    dup
    cover 3
    // smart_contracts/keeper/contract.py:323-325
    // # Opt-in first, because reading a balance or a freeze flag for an
    // # account that never opted in fails rather than answering.
    // if bonus > 0 and not Txn.sender.is_opted_in(Asset(bonus_asset)):
    bz cancel_after_if_else@4
    txn Sender
    dig 4
    asset_holding_get AssetBalance
    bury 1
    bnz cancel_after_if_else@4
    // smart_contracts/keeper/contract.py:326-328
    // # Cancelling still returns the ALGO. Forcing an opt-in first would
    // # let an asset the creator cannot hold block their own refund.
    // bonus = UInt64(0)
    intc_1 // 0
    bury 3

cancel_after_if_else@4:
    // smart_contracts/keeper/contract.py:329
    // if bonus > 0:
    dig 2
    bz cancel_after_if_else@8
    // smart_contracts/keeper/contract.py:330
    // held: UInt64 = Asset(bonus_asset).balance(Global.current_application_address)
    global CurrentApplicationAddress
    dig 4
    asset_holding_get AssetBalance
    swap
    dup
    cover 2
    bury 7
    assert // account opted into asset
    // smart_contracts/keeper/contract.py:331
    // if held < bonus:
    dig 3
    <
    bz cancel_after_if_else@8
    dig 4
    bury 3

cancel_after_if_else@8:
    // smart_contracts/keeper/contract.py:333-336
    // if bonus > 0 and (
    //     Asset(bonus_asset).frozen(Global.current_application_address)
    //     or Asset(bonus_asset).frozen(Txn.sender)
    // ):
    dig 2
    bz cancel_after_if_else@12
    // smart_contracts/keeper/contract.py:334
    // Asset(bonus_asset).frozen(Global.current_application_address)
    global CurrentApplicationAddress
    dig 4
    asset_holding_get AssetFrozen
    assert // account opted into asset
    // smart_contracts/keeper/contract.py:334-335
    // Asset(bonus_asset).frozen(Global.current_application_address)
    // or Asset(bonus_asset).frozen(Txn.sender)
    bnz cancel_if_body@11
    // smart_contracts/keeper/contract.py:335
    // or Asset(bonus_asset).frozen(Txn.sender)
    txn Sender
    dig 4
    asset_holding_get AssetFrozen
    assert // account opted into asset
    // smart_contracts/keeper/contract.py:334-335
    // Asset(bonus_asset).frozen(Global.current_application_address)
    // or Asset(bonus_asset).frozen(Txn.sender)
    bz cancel_after_if_else@12

cancel_if_body@11:
    // smart_contracts/keeper/contract.py:337-343
    // # A frozen holding still reports a balance, so checking the amount
    // # is not enough. The transfer would fail with "asset frozen in
    // # recipient", and it shares a transaction with the ALGO refund, so
    // # the creator would lose their escrow and box minimum balance for
    // # good. They cannot even escape by opting out: a frozen account
    // # cannot close an asset holding.
    // bonus = UInt64(0)
    intc_1 // 0
    bury 3

cancel_after_if_else@12:
    // smart_contracts/keeper/contract.py:347
    // upkeep.balance.as_uint64()
    swap
    dup
    pushint 66
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    // smart_contracts/keeper/contract.py:348
    // + BOX_MBR_FIXED
    intc 7 // 58100
    // smart_contracts/keeper/contract.py:347-348
    // upkeep.balance.as_uint64()
    // + BOX_MBR_FIXED
    +
    // smart_contracts/keeper/contract.py:349
    // + 400 * upkeep.call_args.bytes.length
    uncover 2
    dup
    pushint 40
    extract_uint16
    dig 1
    len
    substring3
    len
    pushint 400
    *
    // smart_contracts/keeper/contract.py:347-349
    // upkeep.balance.as_uint64()
    // + BOX_MBR_FIXED
    // + 400 * upkeep.call_args.bytes.length
    +
    dup
    uncover 2
    // smart_contracts/keeper/contract.py:351
    // del box.value
    box_del
    pop
    // smart_contracts/keeper/contract.py:352
    // itxn.Payment(receiver=Txn.sender, amount=refund).submit()
    itxn_begin
    txn Sender
    itxn_field Receiver
    itxn_field Amount
    intc_2 // pay
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    itxn_submit
    // smart_contracts/keeper/contract.py:353
    // if bonus > 0:
    dig 1
    bz cancel_after_if_else@16
    // smart_contracts/keeper/contract.py:354-358
    // itxn.AssetTransfer(
    //     xfer_asset=Asset(bonus_asset),
    //     asset_receiver=Txn.sender,
    //     asset_amount=bonus,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:356
    // asset_receiver=Txn.sender,
    txn Sender
    dig 2
    itxn_field AssetAmount
    itxn_field AssetReceiver
    dig 2
    itxn_field XferAsset
    // smart_contracts/keeper/contract.py:354
    // itxn.AssetTransfer(
    pushint 4 // axfer
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:354-358
    // itxn.AssetTransfer(
    //     xfer_asset=Asset(bonus_asset),
    //     asset_receiver=Txn.sender,
    //     asset_amount=bonus,
    // ).submit()
    itxn_submit

cancel_after_if_else@16:
    // smart_contracts/keeper/contract.py:296
    // @abimethod()
    itob
    bytec_1 // 0x151f7c75
    swap
    concat
    log
    intc_2 // 1
    return


// smart_contracts.keeper.contract.Keeper.execute[routing]() -> void:
execute:
    intc_1 // 0
    dupn 2
    pushbytes ""
    dupn 7
    // smart_contracts/keeper/contract.py:361
    // @abimethod()
    txna ApplicationArgs 1
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    // smart_contracts/keeper/contract.py:367
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    itob
    bytec_0 // 0x75
    swap
    concat
    dupn 2
    // smart_contracts/keeper/contract.py:368
    // assert box, "Upkeep not found"
    box_len
    bury 1
    assert // Upkeep not found
    // smart_contracts/keeper/contract.py:369
    // upkeep = box.value.copy()
    dup
    box_get
    pop
    cover 2
    // smart_contracts/keeper/contract.py:369-370
    // upkeep = box.value.copy()
    // due: UInt64 = upkeep.next_execution_round.as_uint64()
    dup
    pushint 50
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:370
    // due: UInt64 = upkeep.next_execution_round.as_uint64()
    btoi
    dup
    cover 3
    // smart_contracts/keeper/contract.py:371
    // assert Global.round >= due, "Not due"
    global Round
    <=
    assert // Not due
    // smart_contracts/keeper/contract.py:373
    // interval: UInt64 = upkeep.interval_rounds.as_uint64()
    dup
    pushint 42
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    cover 3
    btoi
    cover 2
    // smart_contracts/keeper/contract.py:374
    // base: UInt64 = upkeep.fee_per_execution.as_uint64()
    dup
    pushint 58
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    cover 3
    btoi
    dup
    cover 2
    cover 3
    // smart_contracts/keeper/contract.py:375
    // cap: UInt64 = upkeep.fee_cap.as_uint64()
    pushint 90
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    cover 3
    btoi
    dup
    uncover 2
    // smart_contracts/keeper/contract.py:377-390
    // # `due > last_serviced_round` means this upkeep was on schedule the
    // # last time it ran, so being late now is genuine neglect. When it is
    // # false the call is a replay of a backlog, and a replay never pays
    // # more than base.
    // #
    // # Both halves are needed. Measuring lateness from the last service is
    // # what stops a burst drained in one go from paying the ceiling on
    // # every replay. On its own it is not enough: under CATCH_UP a replay
    // # only advances the schedule by one interval, so a keeper that waits
    // # two intervals between replays is late again by its own measure, and
    // # collects the ceiling every time while the backlog grows without
    // # bound. Measured: 34 runs took 100% of a 400,000 µALGO escrow and
    // # left the upkeep 5,400 rounds further behind than it started.
    // if cap > base and due > upkeep.last_serviced_round.as_uint64():
    >
    bnz execute_and_contd@2
    pop
    dig 2
    bury 13

execute_after_if_else@10:
    // smart_contracts/keeper/contract.py:408
    // assert upkeep.balance.as_uint64() >= fee, "Insufficient funding"
    dupn 2
    pushint 66
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    dup
    cover 2
    dig 15
    >=
    assert // Insufficient funding
    // smart_contracts/keeper/contract.py:410
    // next_due: UInt64 = due + interval
    dig 8
    dig 7
    +
    bury 12
    // smart_contracts/keeper/contract.py:411
    // if upkeep.policy.as_uint64() == SKIP_AHEAD:
    pushint 82
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    bury 20
    btoi
    intc_2 // 1
    ==
    bz execute_after_if_else@12
    // smart_contracts/keeper/contract.py:412-415
    // # Snap to the first slot strictly in the future, keeping the
    // # schedule's phase: a daily upkeep stays on its time of day rather
    // # than drifting to whenever a keeper happened to arrive.
    // missed: UInt64 = (Global.round - due) // interval
    global Round
    dig 8
    dup
    cover 2
    -
    dig 7
    dup
    cover 2
    /
    // smart_contracts/keeper/contract.py:416
    // next_due = due + (missed + 1) * interval
    intc_2 // 1
    +
    *
    +
    bury 11

execute_after_if_else@12:
    // smart_contracts/keeper/contract.py:417
    // new_balance: UInt64 = upkeep.balance.as_uint64() - fee
    dig 13
    -
    bury 11
    // smart_contracts/keeper/contract.py:418
    // times: UInt64 = upkeep.times_executed.as_uint64() + 1
    dupn 2
    pushint 74
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    intc_2 // 1
    +
    bury 10
    // smart_contracts/keeper/contract.py:420-425
    // # The ASA bonus is paid only when there is one, the escrow can cover it
    // # and the keeper can receive it. A keeper that has not opted in is not
    // # a failed execution — it takes the full ALGO fee and forfeits the
    // # bonus, which stays in escrow for the creator. Reverting instead would
    // # quietly shrink the keeper set for exactly the upkeeps paying extra.
    // bonus_asset: UInt64 = upkeep.fee_asset.as_uint64()
    dup
    pushint 106
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    bury 19
    btoi
    dup
    cover 2
    bury 17
    // smart_contracts/keeper/contract.py:426
    // bonus: UInt64 = upkeep.asset_fee.as_uint64()
    dup
    pushint 114
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    bury 22
    btoi
    bury 18
    // smart_contracts/keeper/contract.py:427
    // asset_balance: UInt64 = upkeep.asset_balance.as_uint64()
    pushint 122
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    swap
    // smart_contracts/keeper/contract.py:436-441
    // bonus_asset > 0
    // and asset_balance >= bonus
    // and Txn.sender.is_opted_in(Asset(bonus_asset))
    // and Asset(bonus_asset).balance(Global.current_application_address) >= bonus
    // and not Asset(bonus_asset).frozen(Global.current_application_address)
    // and not Asset(bonus_asset).frozen(Txn.sender)
    bz execute_bool_false@19
    // smart_contracts/keeper/contract.py:437
    // and asset_balance >= bonus
    dup
    dig 17
    >=
    // smart_contracts/keeper/contract.py:436-441
    // bonus_asset > 0
    // and asset_balance >= bonus
    // and Txn.sender.is_opted_in(Asset(bonus_asset))
    // and Asset(bonus_asset).balance(Global.current_application_address) >= bonus
    // and not Asset(bonus_asset).frozen(Global.current_application_address)
    // and not Asset(bonus_asset).frozen(Txn.sender)
    bz execute_bool_false@19
    // smart_contracts/keeper/contract.py:438
    // and Txn.sender.is_opted_in(Asset(bonus_asset))
    txn Sender
    dig 16
    asset_holding_get AssetBalance
    bury 1
    // smart_contracts/keeper/contract.py:436-441
    // bonus_asset > 0
    // and asset_balance >= bonus
    // and Txn.sender.is_opted_in(Asset(bonus_asset))
    // and Asset(bonus_asset).balance(Global.current_application_address) >= bonus
    // and not Asset(bonus_asset).frozen(Global.current_application_address)
    // and not Asset(bonus_asset).frozen(Txn.sender)
    bz execute_bool_false@19
    // smart_contracts/keeper/contract.py:439
    // and Asset(bonus_asset).balance(Global.current_application_address) >= bonus
    global CurrentApplicationAddress
    dig 16
    asset_holding_get AssetBalance
    assert // account opted into asset
    dig 17
    >=
    // smart_contracts/keeper/contract.py:436-441
    // bonus_asset > 0
    // and asset_balance >= bonus
    // and Txn.sender.is_opted_in(Asset(bonus_asset))
    // and Asset(bonus_asset).balance(Global.current_application_address) >= bonus
    // and not Asset(bonus_asset).frozen(Global.current_application_address)
    // and not Asset(bonus_asset).frozen(Txn.sender)
    bz execute_bool_false@19
    // smart_contracts/keeper/contract.py:440
    // and not Asset(bonus_asset).frozen(Global.current_application_address)
    global CurrentApplicationAddress
    dig 16
    asset_holding_get AssetFrozen
    assert // account opted into asset
    bnz execute_bool_false@19
    // smart_contracts/keeper/contract.py:441
    // and not Asset(bonus_asset).frozen(Txn.sender)
    txn Sender
    dig 16
    asset_holding_get AssetFrozen
    assert // account opted into asset
    bnz execute_bool_false@19
    intc_2 // 1

execute_bool_merge@20:
    // smart_contracts/keeper/contract.py:443
    // if pays_bonus:
    dup
    bz execute_after_if_else@22
    // smart_contracts/keeper/contract.py:444
    // asset_balance = asset_balance - bonus
    swap
    dig 17
    -
    swap

execute_after_if_else@22:
    // smart_contracts/keeper/contract.py:446-453
    // box.value = upkeep._replace(
    //     next_execution_round=arc4.UInt64(next_due),
    //     balance=arc4.UInt64(new_balance),
    //     times_executed=arc4.UInt64(times),
    //     last_serviced_round=arc4.UInt64(Global.round),
    //     asset_balance=arc4.UInt64(asset_balance),
    //     call_args=upkeep.call_args.copy(),
    // )
    dig 2
    dup
    intc_1 // 0
    pushint 32
    box_extract // on error: index out of bounds
    dig 1
    pushint 32
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:452
    // call_args=upkeep.call_args.copy(),
    dig 12
    dup
    pushint 40
    extract_uint16
    dig 1
    len
    substring3
    // smart_contracts/keeper/contract.py:447
    // next_execution_round=arc4.UInt64(next_due),
    dig 15
    itob
    dup
    cover 6
    // smart_contracts/keeper/contract.py:448
    // balance=arc4.UInt64(new_balance),
    dig 18
    itob
    // smart_contracts/keeper/contract.py:449
    // times_executed=arc4.UInt64(times),
    dig 17
    itob
    // smart_contracts/keeper/contract.py:450
    // last_serviced_round=arc4.UInt64(Global.round),
    global Round
    itob
    // smart_contracts/keeper/contract.py:451
    // asset_balance=arc4.UInt64(asset_balance),
    uncover 10
    itob
    // smart_contracts/keeper/contract.py:446-453
    // box.value = upkeep._replace(
    //     next_execution_round=arc4.UInt64(next_due),
    //     balance=arc4.UInt64(new_balance),
    //     times_executed=arc4.UInt64(times),
    //     last_serviced_round=arc4.UInt64(Global.round),
    //     asset_balance=arc4.UInt64(asset_balance),
    //     call_args=upkeep.call_args.copy(),
    // )
    uncover 7
    dig 7
    concat
    bytec_3 // 0x0082
    concat
    dig 16
    concat
    uncover 5
    concat
    dig 13
    concat
    uncover 4
    concat
    uncover 3
    concat
    dig 25
    concat
    dig 9
    concat
    uncover 2
    concat
    dig 23
    concat
    dig 25
    concat
    swap
    concat
    swap
    concat
    dig 2
    box_del
    pop
    dig 2
    swap
    box_put
    // smart_contracts/keeper/contract.py:455-460
    // # One static branch per argument count. Building the array in a loop
    // # compiles but does not work: Puya models `app_args` as
    // # compile-time-numbered slots and hoists the inner transaction out of
    // # the loop, so only the last assignment survives. `register` bounds the
    // # count, so the final branch is unreachable.
    // target: UInt64 = upkeep.target_app.as_uint64()
    btoi
    cover 3
    // smart_contracts/keeper/contract.py:461
    // arg_count: UInt64 = upkeep.call_args.length
    intc 5 // 130
    intc_3 // 2
    box_extract
    btoi
    dup
    // smart_contracts/keeper/contract.py:462
    // if arg_count == 1:
    intc_2 // 1
    ==
    bz execute_else_body@25
    pop
    // smart_contracts/keeper/contract.py:463-467
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:465
    // app_args=(upkeep.call_args[0].native,),
    uncover 3
    dup
    intc 5 // 130
    intc_3 // 2
    box_extract
    btoi
    assert // index out of bounds
    dup
    intc 4 // 132
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dup2
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    box_extract
    extract 2 0
    // smart_contracts/keeper/contract.py:466
    // on_completion=OnCompleteAction.NoOp,
    intc_1 // NoOp
    itxn_field OnCompletion
    itxn_field ApplicationArgs
    uncover 2
    itxn_field ApplicationID
    // smart_contracts/keeper/contract.py:463
    // itxn.ApplicationCall(
    pushint 6 // appl
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:463-467
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_submit

execute_after_if_else@34:
    // smart_contracts/keeper/contract.py:482
    // itxn.Payment(receiver=Txn.sender, amount=fee).submit()
    itxn_begin
    txn Sender
    dig 14
    itxn_field Amount
    itxn_field Receiver
    intc_2 // pay
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    itxn_submit
    // smart_contracts/keeper/contract.py:483
    // if pays_bonus:
    bz execute_after_if_else@38
    // smart_contracts/keeper/contract.py:484-488
    // itxn.AssetTransfer(
    //     xfer_asset=Asset(bonus_asset),
    //     asset_receiver=Txn.sender,
    //     asset_amount=bonus,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:486
    // asset_receiver=Txn.sender,
    txn Sender
    dig 16
    itxn_field AssetAmount
    itxn_field AssetReceiver
    dig 14
    itxn_field XferAsset
    // smart_contracts/keeper/contract.py:484
    // itxn.AssetTransfer(
    pushint 4 // axfer
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:484-488
    // itxn.AssetTransfer(
    //     xfer_asset=Asset(bonus_asset),
    //     asset_receiver=Txn.sender,
    //     asset_amount=bonus,
    // ).submit()
    itxn_submit

execute_after_if_else@38:
    // smart_contracts/keeper/contract.py:361
    // @abimethod()
    bytec_1 // 0x151f7c75
    swap
    concat
    log
    intc_2 // 1
    return

execute_else_body@25:
    // smart_contracts/keeper/contract.py:468
    // elif arg_count == 2:
    dup
    intc_3 // 2
    ==
    bz execute_else_body@28
    pop
    // smart_contracts/keeper/contract.py:469-473
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:471
    // app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native,),
    uncover 3
    dup
    intc 5 // 130
    intc_3 // 2
    box_extract
    btoi
    dup
    assert // index out of bounds
    dig 1
    intc 4 // 132
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dig 2
    dig 1
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    dig 3
    cover 2
    box_extract
    extract 2 0
    intc_2 // 1
    uncover 2
    <
    assert // index out of bounds
    dig 1
    pushint 134
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dig 2
    dig 1
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    uncover 3
    cover 2
    box_extract
    extract 2 0
    // smart_contracts/keeper/contract.py:472
    // on_completion=OnCompleteAction.NoOp,
    intc_1 // NoOp
    itxn_field OnCompletion
    swap
    itxn_field ApplicationArgs
    itxn_field ApplicationArgs
    uncover 2
    itxn_field ApplicationID
    // smart_contracts/keeper/contract.py:469
    // itxn.ApplicationCall(
    pushint 6 // appl
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:469-473
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_submit
    b execute_after_if_else@34

execute_else_body@28:
    // smart_contracts/keeper/contract.py:474
    // elif arg_count == 3:
    pushint 3
    ==
    assert // Unsupported argument count
    // smart_contracts/keeper/contract.py:475-479
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native, upkeep.call_args[2].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:477
    // app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native, upkeep.call_args[2].native,),
    uncover 3
    dup
    intc 5 // 130
    intc_3 // 2
    box_extract
    btoi
    dup
    assert // index out of bounds
    dig 1
    intc 4 // 132
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dig 2
    dig 1
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    dig 3
    cover 2
    box_extract
    extract 2 0
    intc_2 // 1
    dig 2
    <
    assert // index out of bounds
    dig 2
    pushint 134
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dig 3
    dig 1
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    dig 4
    cover 2
    box_extract
    extract 2 0
    intc_3 // 2
    uncover 3
    <
    assert // index out of bounds
    dig 2
    pushint 136
    intc_3 // 2
    box_extract
    btoi
    intc 4 // 132
    +
    dig 3
    dig 1
    intc_3 // 2
    box_extract
    btoi
    intc_3 // 2
    +
    uncover 4
    cover 2
    box_extract
    extract 2 0
    // smart_contracts/keeper/contract.py:478
    // on_completion=OnCompleteAction.NoOp,
    intc_1 // NoOp
    itxn_field OnCompletion
    uncover 2
    itxn_field ApplicationArgs
    swap
    itxn_field ApplicationArgs
    itxn_field ApplicationArgs
    uncover 2
    itxn_field ApplicationID
    // smart_contracts/keeper/contract.py:475
    // itxn.ApplicationCall(
    pushint 6 // appl
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:475-479
    // itxn.ApplicationCall(
    //     app_id=target,
    //     app_args=(upkeep.call_args[0].native, upkeep.call_args[1].native, upkeep.call_args[2].native,),
    //     on_completion=OnCompleteAction.NoOp,
    // ).submit()
    itxn_submit
    b execute_after_if_else@34

execute_bool_false@19:
    intc_1 // 0
    b execute_bool_merge@20

execute_and_contd@2:
    // smart_contracts/keeper/contract.py:377-390
    // # `due > last_serviced_round` means this upkeep was on schedule the
    // # last time it ran, so being late now is genuine neglect. When it is
    // # false the call is a replay of a backlog, and a replay never pays
    // # more than base.
    // #
    // # Both halves are needed. Measuring lateness from the last service is
    // # what stops a burst drained in one go from paying the ceiling on
    // # every replay. On its own it is not enough: under CATCH_UP a replay
    // # only advances the schedule by one interval, so a keeper that waits
    // # two intervals between replays is late again by its own measure, and
    // # collects the ceiling every time while the backlog grows without
    // # bound. Measured: 34 runs took 100% of a 400,000 µALGO escrow and
    // # left the upkeep 5,400 rounds further behind than it started.
    // if cap > base and due > upkeep.last_serviced_round.as_uint64():
    dig 1
    pushint 98
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    dup
    dig 9
    <
    bnz execute_if_body@3
    popn 2
    dig 2
    bury 13
    b execute_after_if_else@10

execute_if_body@3:
    // smart_contracts/keeper/contract.py:391-393
    // # Escalation exists to clear a market; once a keeper has arrived
    // # the market has cleared.
    // lateness: UInt64 = Global.round - upkeep.last_serviced_round.as_uint64()
    global Round
    swap
    -
    dup
    bury 14
    // smart_contracts/keeper/contract.py:394
    // excess: UInt64 = UInt64(0)
    intc_1 // 0
    bury 16
    // smart_contracts/keeper/contract.py:395
    // if lateness > interval:
    dig 6
    >
    bz execute_after_if_else@5
    // smart_contracts/keeper/contract.py:396
    // excess = lateness - interval
    dig 12
    dig 6
    -
    bury 15

execute_after_if_else@5:
    // smart_contracts/keeper/contract.py:397
    // if excess > interval:
    dig 14
    dig 6
    >
    bz execute_after_if_else@7
    dig 5
    bury 15

execute_after_if_else@7:
    // smart_contracts/keeper/contract.py:399-400
    // # Linear from base to cap over one missed interval, then flat.
    // fee = base + (cap - base) * excess // interval
    dig 3
    dup
    cover 2
    -
    dig 15
    *
    dig 6
    /
    +
    dup
    bury 14
    // smart_contracts/keeper/contract.py:401-406
    // # An upkeep can only bid what it holds. Without this, an escrow
    // # that has fallen below the escalated fee freezes the upkeep for
    // # good — lateness only grows, so the price it cannot pay only
    // # rises. Dropping back to the base fee keeps it executable by
    // # anyone until the escrow is genuinely empty.
    // if upkeep.balance.as_uint64() < fee:
    dig 1
    pushint 66
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    >
    bz execute_after_if_else@10
    dig 2
    bury 13
    b execute_after_if_else@10


// smart_contracts.keeper.contract.Keeper.opt_in_asset[routing]() -> void:
opt_in_asset:
    // smart_contracts/keeper/contract.py:491
    // @abimethod()
    txn GroupIndex
    intc_2 // 1
    -
    dup
    gtxns TypeEnum
    intc_2 // pay
    ==
    assert // transaction type is pay
    txna ApplicationArgs 1
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    txna ApplicationArgs 2
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    // smart_contracts/keeper/contract.py:503
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    swap
    itob
    bytec_0 // 0x75
    swap
    concat
    // smart_contracts/keeper/contract.py:504
    // assert box, "Upkeep not found"
    dup
    box_len
    bury 1
    assert // Upkeep not found
    // smart_contracts/keeper/contract.py:506
    // box.value.fee_asset.as_uint64() == asset.id
    pushint 106
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    dig 1
    ==
    // smart_contracts/keeper/contract.py:505-507
    // assert (
    //     box.value.fee_asset.as_uint64() == asset.id
    // ), "That upkeep does not use this asset"
    assert // That upkeep does not use this asset
    // smart_contracts/keeper/contract.py:509
    // mbr_payment.receiver == Global.current_application_address
    dig 1
    gtxns Receiver
    global CurrentApplicationAddress
    ==
    // smart_contracts/keeper/contract.py:508-510
    // assert (
    //     mbr_payment.receiver == Global.current_application_address
    // ), "MBR payment must fund the app account"
    assert // MBR payment must fund the app account
    // smart_contracts/keeper/contract.py:511-513
    // # The deposit is not refundable, so a payment somebody else signed is
    // # money taken from them and kept, with nothing crediting them for it.
    // assert mbr_payment.sender == Txn.sender, "MBR payment must come from the caller"
    dig 1
    gtxns Sender
    txn Sender
    ==
    assert // MBR payment must come from the caller
    // smart_contracts/keeper/contract.py:514
    // assert mbr_payment.rekey_to == Global.zero_address, "MBR payment must not rekey"
    dig 1
    gtxns RekeyTo
    global ZeroAddress
    ==
    assert // MBR payment must not rekey
    // smart_contracts/keeper/contract.py:516
    // mbr_payment.close_remainder_to == Global.zero_address
    dig 1
    gtxns CloseRemainderTo
    global ZeroAddress
    ==
    // smart_contracts/keeper/contract.py:515-517
    // assert (
    //     mbr_payment.close_remainder_to == Global.zero_address
    // ), "MBR payment must not close"
    assert // MBR payment must not close
    // smart_contracts/keeper/contract.py:518
    // assert mbr_payment.amount >= ASSET_OPT_IN_MBR, "MBR payment too small"
    swap
    gtxns Amount
    pushint 100000
    >=
    assert // MBR payment too small
    // smart_contracts/keeper/contract.py:519-523
    // itxn.AssetTransfer(
    //     xfer_asset=asset,
    //     asset_receiver=Global.current_application_address,
    //     asset_amount=0,
    // ).submit()
    itxn_begin
    // smart_contracts/keeper/contract.py:521
    // asset_receiver=Global.current_application_address,
    global CurrentApplicationAddress
    // smart_contracts/keeper/contract.py:522
    // asset_amount=0,
    intc_1 // 0
    itxn_field AssetAmount
    itxn_field AssetReceiver
    itxn_field XferAsset
    // smart_contracts/keeper/contract.py:519
    // itxn.AssetTransfer(
    pushint 4 // axfer
    itxn_field TypeEnum
    intc_1 // 0
    itxn_field Fee
    // smart_contracts/keeper/contract.py:519-523
    // itxn.AssetTransfer(
    //     xfer_asset=asset,
    //     asset_receiver=Global.current_application_address,
    //     asset_amount=0,
    // ).submit()
    itxn_submit
    // smart_contracts/keeper/contract.py:491
    // @abimethod()
    pushbytes 0x151f7c7500000000000186a0
    log
    intc_2 // 1
    return


// smart_contracts.keeper.contract.Keeper.top_up_asset[routing]() -> void:
top_up_asset:
    // smart_contracts/keeper/contract.py:526
    // @abimethod()
    txna ApplicationArgs 1
    dup
    len
    intc_0 // 8
    ==
    assert // invalid number of bytes for arc4.uint64
    btoi
    txn GroupIndex
    intc_2 // 1
    -
    dup
    gtxns TypeEnum
    pushint 4 // axfer
    ==
    assert // transaction type is axfer
    // smart_contracts/keeper/contract.py:537
    // box = Box(Upkeep, key=op.concat(b"u", op.itob(upkeep_id)))
    swap
    itob
    bytec_0 // 0x75
    swap
    concat
    // smart_contracts/keeper/contract.py:538
    // assert box, "Upkeep not found"
    dup
    box_len
    bury 1
    assert // Upkeep not found
    // smart_contracts/keeper/contract.py:539
    // upkeep = box.value.copy()
    dup
    box_get
    pop
    cover 2
    // smart_contracts/keeper/contract.py:541
    // asset_funding.asset_receiver == Global.current_application_address
    dig 1
    gtxns AssetReceiver
    global CurrentApplicationAddress
    ==
    // smart_contracts/keeper/contract.py:540-542
    // assert (
    //     asset_funding.asset_receiver == Global.current_application_address
    // ), "Asset funding must go to the app account"
    assert // Asset funding must go to the app account
    // smart_contracts/keeper/contract.py:546
    // asset_funding.sender == Txn.sender
    dig 1
    gtxns Sender
    txn Sender
    ==
    // smart_contracts/keeper/contract.py:543-547
    // # Same reason as `top_up`: a bonus somebody else's transfer paid for
    // # becomes the upkeep creator's to spend, and only they can cancel.
    // assert (
    //     asset_funding.sender == Txn.sender
    // ), "Asset funding must come from the caller"
    assert // Asset funding must come from the caller
    // smart_contracts/keeper/contract.py:548
    // assert asset_funding.rekey_to == Global.zero_address, "Asset funding must not rekey"
    dig 1
    gtxns RekeyTo
    global ZeroAddress
    ==
    assert // Asset funding must not rekey
    // smart_contracts/keeper/contract.py:550
    // asset_funding.asset_close_to == Global.zero_address
    dig 1
    gtxns AssetCloseTo
    global ZeroAddress
    ==
    // smart_contracts/keeper/contract.py:549-551
    // assert (
    //     asset_funding.asset_close_to == Global.zero_address
    // ), "Asset funding must not close the asset"
    assert // Asset funding must not close the asset
    // smart_contracts/keeper/contract.py:553
    // asset_funding.xfer_asset.id == upkeep.fee_asset.as_uint64()
    dig 1
    gtxns XferAsset
    dig 1
    pushint 106
    intc_0 // 8
    box_extract // on error: index out of bounds
    dup
    btoi
    uncover 2
    ==
    // smart_contracts/keeper/contract.py:552-554
    // assert (
    //     asset_funding.xfer_asset.id == upkeep.fee_asset.as_uint64()
    // ), "Wrong asset for this upkeep"
    assert // Wrong asset for this upkeep
    // smart_contracts/keeper/contract.py:556
    // upkeep.asset_balance.as_uint64() + asset_funding.asset_amount
    dig 1
    pushint 122
    intc_0 // 8
    box_extract // on error: index out of bounds
    btoi
    uncover 3
    gtxns AssetAmount
    +
    // smart_contracts/keeper/contract.py:558-561
    // box.value = upkeep._replace(
    //     asset_balance=arc4.UInt64(new_asset_balance),
    //     call_args=upkeep.call_args.copy(),
    // )
    dig 2
    intc_1 // 0
    pushint 32
    box_extract // on error: index out of bounds
    dig 3
    pushint 32
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:560
    // call_args=upkeep.call_args.copy(),
    dig 5
    pushint 40
    extract_uint16
    dig 6
    len
    uncover 7
    cover 2
    substring3
    // smart_contracts/keeper/contract.py:558-561
    // box.value = upkeep._replace(
    //     asset_balance=arc4.UInt64(new_asset_balance),
    //     call_args=upkeep.call_args.copy(),
    // )
    dig 5
    pushint 42
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 6
    pushint 50
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 7
    pushint 58
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 8
    pushint 66
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 9
    pushint 74
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 10
    pushint 82
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 11
    pushint 90
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 12
    pushint 98
    intc_0 // 8
    box_extract // on error: index out of bounds
    dig 13
    pushint 114
    intc_0 // 8
    box_extract // on error: index out of bounds
    // smart_contracts/keeper/contract.py:559
    // asset_balance=arc4.UInt64(new_asset_balance),
    uncover 12
    itob
    // smart_contracts/keeper/contract.py:558-561
    // box.value = upkeep._replace(
    //     asset_balance=arc4.UInt64(new_asset_balance),
    //     call_args=upkeep.call_args.copy(),
    // )
    uncover 12
    uncover 12
    concat
    bytec_3 // 0x0082
    concat
    uncover 10
    concat
    uncover 9
    concat
    uncover 8
    concat
    uncover 7
    concat
    uncover 6
    concat
    uncover 5
    concat
    uncover 4
    concat
    uncover 3
    concat
    uncover 4
    concat
    uncover 2
    concat
    dig 1
    concat
    uncover 2
    concat
    dig 2
    box_del
    pop
    uncover 2
    swap
    box_put
    // smart_contracts/keeper/contract.py:526
    // @abimethod()
    bytec_1 // 0x151f7c75
    swap
    concat
    log
    intc_2 // 1
    return
